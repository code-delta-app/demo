# Build-Proj

Stress fixture for the CodeDelta build-file change alert.
An expression evaluator (C) with a small job control plane (Flask).

Version 1.0: make/CMake builds, Jenkins + GitLab CI, RPM packaging.
