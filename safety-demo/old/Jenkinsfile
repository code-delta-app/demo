// Meridian Helpdesk — Jenkins pipeline. Synthetic sample, never executed.
pipeline {
  agent any
  stages {
    stage('test') { steps { sh 'python3 -m pytest -q tests' } }
    stage('autofix') { steps { sh 'codex exec --full-auto "fix the failing tests in helpdesk/"' } }
  }
}
